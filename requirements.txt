# Doc-SCoT dependencies.
# Versions below are those of the environment this release was developed and evaluated in.
# The code targets transformers 5.x (Qwen3VLForConditionalGeneration) together with torch 2.5.1;
# the two have a known torch.load compatibility conflict, worked around in train.py
# (see "Notes on the environment" in README.md).

# --- core ---
torch==2.5.1
torchvision==0.20.1
transformers==5.14.1
accelerate==1.3.0
peft==0.10.0
deepspeed==0.16.4
bitsandbytes==0.43.1
huggingface_hub==1.25.1
safetensors
qwen-vl-utils==0.0.14

# --- data / metrics ---
numpy==1.26.4
opencv-python==4.10.0.84
pillow==10.2.0
ujson==5.10.0
pyyaml
tqdm

# --- vision specialists (teachers, training only) ---
python-doctr==1.0.0
doclayout_yolo==0.0.4

# --- logging / demo ---
wandb==0.16.6
gradio==6.1.0
scikit-learn==1.5.2
matplotlib==3.10.1
